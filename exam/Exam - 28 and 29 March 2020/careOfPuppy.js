function careOfPuppy(input) {
    let amountOfFoodKg = Number(input[0]);
    let dailyFoodGram = 0;
    let foodGram = amountOfFoodKg * 1000;


    while (i !== 'Adopted') {
    
}
  //  for (let i = 1; i < input.length; i++) {
        if (dailyFoodGram !== 'Adopted') {
            dailyFoodGram += Number(input[i]);

        }

    }

    console.log(dailyFoodGram);
}

careOfPuppy([4,
    130,
    345,
    400,
    180,
    230,
    120,
    "Adopted"])